server:
  port: 8082

spring:
  application:
    name: auth-service

  banner:
    location: banner.txt

  datasource:
    url: jdbc:postgresql://localhost:5432/omni-bank-dev
    username: postgres
    password: postgres
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: create
